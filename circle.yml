machine:
  services:
    - docker

dependencies:
  override:
    - docker info

test:
  override:
    - docker build -t phpbcom/postgresql .
    - docker run -d --name=postgresql phpbcom/postgresql; sleep 10
    - docker run -it --volumes-from=postgresql phpbcom/postgresql sudo -u postgres -H psql -c "\conninfo"
